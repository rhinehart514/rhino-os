---
name: calibrate
description: "Ground taste eval in YOUR preferences — founder interview, design system docs, dimension research, drift detection. /calibrate [profile|design-system|dimensions|verify|refresh|drift]"
---

# /calibrate

This command is handled by the `calibrate` skill. Run `/calibrate` to invoke it.

$ARGUMENTS
