---
name: openclaw
description: "Chat-optimized interface — pulse, ask, nudge, bet, grunt. Formats rhino-os output for messaging delivery. /openclaw [pulse|ask|nudge|bet|grunt]"
---

# /openclaw

This command is handled by the `openclaw` skill. Run `/openclaw` to invoke it.

$ARGUMENTS
